{
  "$schema": "https://pages.edgeone.ai/schema.json",
  "framework": "none",
  "buildCommand": "pnpm --filter taibu-core build && mkdir -p .next && echo '{\"version\":1,\"config\":{\"output\":\"standalone\"},\"appDir\":true,\"files\":[],\"ignore\":[]}' > .next/required-server-files.json",
  "installCommand": "pnpm install",
  "outputDirectory": ".",
  "cloudFunctions": {
    "runtime": "nodejs20.x"
  }
}
