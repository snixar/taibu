{
  "$schema": "https://pages.edgeone.ai/schema.json",
  "buildCommand": "pnpm build",
  "installCommand": "pnpm install",
  "outputDirectory": ".next/standalone",
  "cloudFunctions": {
    "runtime": "nodejs20.x",
    "external": ["circular-natal-horoscope-js"]
  }
}
